# CLAUDE.md

## Git 提交身份

本项目所有 `git commit` / `git push` **统一使用 `cookaihq` 身份**，无需再向用户确认：

- `user.name`: `cookaihq`
- `user.email`: `263483889+cookaihq@users.noreply.github.com`
- remote: `https://github.com/cookaihq/awesome-skills.git`

提交前若发现当前仓库 `user.name` / `user.email` 与上述不一致，先用仓库级 `git config` 设回该身份再提交。
